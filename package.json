{
  "name": "cbsd-task-management-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "dev:feature-x": "turbo run dev --filter=feature-x",
    "dev:feature-y": "turbo run dev --filter=feature-y"
  },
  "devDependencies": {
    "turbo": "^2.9.17",
    "typescript": "^6.0.3",
    "prettier": "^3.8.4",
    "@types/node": "^25.9.2"
  },
  "packageManager": "pnpm@11.5.2",
  "engines": {
    "node": ">=18"
  }
}
